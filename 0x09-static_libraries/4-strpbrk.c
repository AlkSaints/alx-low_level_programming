#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>
/**
 * _strpbrk - entry point
 * @s: var
 * @accept: var
 *
 * Return: s, accept
 */
char *_strpbrk(char *s, char *accept)
{
	return (s, accept);
}
